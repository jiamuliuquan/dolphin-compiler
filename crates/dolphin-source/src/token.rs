use crate::source::Span;

#[derive(Clone, Debug, PartialEq, Eq)]
pub enum TokenKind {
    Fn,
    Pub,
    Pkg,
    Use,
    Return,
    Var,
    Val,
    If,
    Else,
    Loop,
    While,
    For,
    In,
    As,
    Break,
    Continue,
    Defer,
    True,
    False,
    Struct,
    Enum,
    Match,
    Extern,
    Trait,
    Impl,
    Identifier(String),
    Number(String),
    Character(char),
    String(String),
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    LeftBracket,
    RightBracket,
    Colon,
    ColonColon,
    Comma,
    Semicolon,
    Plus,
    Minus,
    Star,
    Slash,
    Percent,
    Bang,
    Ampersand,
    Arrow,
    Equal,
    Less,
    Greater,
    PlusEqual,
    MinusEqual,
    StarEqual,
    SlashEqual,
    PercentEqual,
    EqualEqual,
    BangEqual,
    LessEqual,
    GreaterEqual,
    AndAnd,
    OrOr,
    DotDot,
    DotDotEqual,
    FatArrow,
    Underscore,
    Dot,
    Eof,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct Token {
    pub kind: TokenKind,
    pub span: Span,
}
